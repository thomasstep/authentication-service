const { createUser } = require('./createUser');
const { deleteUser } = require('./deleteUser');
const { getUser } = require('./getUser');

module.exports = {
  createUser,
  deleteUser,
  getUser,
};
